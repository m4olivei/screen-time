/**
 * @screen-time/shared — shared data layer and UniFi client.
 *
 * Stub only for now: the TypeORM entities, queries, desired-state function,
 * and UniFi client arrive in later tasks.
 */
export const SHARED_PACKAGE_NAME = '@screen-time/shared';
