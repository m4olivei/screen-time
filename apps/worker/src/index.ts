/**
 * apps/worker — reconcile loop stub.
 *
 * The real worker (tick loop, desired-state reconciliation against UniFi)
 * arrives in a later task.
 */
import { SHARED_PACKAGE_NAME } from '@screen-time/shared';

console.log(`worker stub (using ${SHARED_PACKAGE_NAME})`);
